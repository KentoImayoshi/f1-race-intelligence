[project]
name = "f1-ingestion"
version = "0.1.0"
description = "Placeholder package"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pyarrow>=14.0.0",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
include = ["f1_ingestion*"]
