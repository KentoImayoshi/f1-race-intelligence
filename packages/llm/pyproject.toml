[project]
name = "f1-llm"
version = "0.1.0"
description = "Grounded explanation layer for F1 Race Intelligence"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyarrow>=14.0.0",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
include = ["f1_llm*"]
