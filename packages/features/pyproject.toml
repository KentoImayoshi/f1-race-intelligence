[project]
name = "f1-features"
version = "0.1.0"
description = "Minimal feature engineering for F1 Race Intelligence"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyarrow>=14.0.0",
]

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]
include = ["f1_features*"]
